import { TextPricePipe } from './text-price.pipe';

describe('TextPricePipe', () => {
  it('create an instance', () => {
    const pipe = new TextPricePipe();
    expect(pipe).toBeTruthy();
  });
});
